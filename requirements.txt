torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
tqdm>=4.65.0
pillow>=9.5.0
ftfy>=6.1.0
regex>=2023.0.0
autoattack>=0.1
gdown>=5.0.0
# Note: CLIP is self-contained in replace/ directory; no pip install clip needed
# Verified working: torch 2.5.1, torchvision 0.20.1, autoattack 0.1, Python 3.11
